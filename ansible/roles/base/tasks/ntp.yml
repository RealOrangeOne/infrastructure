- name: "Install NTP"
  apt:
    name: "ntp"
  become: true
  become_user: root

- name: Set timezone
  timezone:
    name: Europe/London
  become: true
  become_user: root

- name: Enable NTP service
  systemd:
    name: ntp
    enabled: true
    state: started
